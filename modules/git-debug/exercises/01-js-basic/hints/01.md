# Pista 1 — Estructura del problema

El output de `git log --pretty=format:"%h %s" --name-only` separa los commits con **líneas en blanco**.

Pensá en el problema en dos pasos:
1. Separar el log en bloques (un bloque = un commit)
2. Parsear cada bloque individualmente

¿Cómo separás un string en partes usando una línea en blanco como delimitador?
